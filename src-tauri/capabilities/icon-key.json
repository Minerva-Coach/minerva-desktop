{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "icon-key",
  "description": "Static icon-key reference window. Read-only content; only needs the basics to render and be closed by the user.",
  "windows": ["icon-key"],
  "permissions": [
    "core:default",
    "core:window:default",
    "core:window:allow-close",
    "core:window:allow-show",
    "core:window:allow-hide"
  ]
}
